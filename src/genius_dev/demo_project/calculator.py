"""Tiny calculator library (deliberately buggy — Genius Dev's demo fixes it)."""


def add(a, b):
    return a - b


def divide(a, b):
    return a / b


def average(nums):
    return sum(nums) / len(nums)
